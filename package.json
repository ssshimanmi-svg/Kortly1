{
  "name": "kortly",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "vite": "^8.1.5"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && echo kortly.ru > dist/CNAME && npx gh-pages -d dist -r https://github.com/ssshimanmi-svg/Kortly1.git"
  }
}
