# Зависимости — ставятся через npm install
node_modules/

# Артефакты сборки — пересоздаются через npm run build / npm run deploy
dist/

# Локальные и системные файлы
.DS_Store
Thumbs.db
*.log
.env
.env.local
